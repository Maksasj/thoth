#include "thoth.h"
#include <iostream>

int main() {
    /*
    {
        thoth::Integer a("1");
        thoth::Integer b("-1");

        std::cout << a.toString() << " < " << b.toString() << " -> " << (a < b) << "\n";
        std::cout << a.toString() << " > " << b.toString() << " -> " << (a > b) << "\n";
        std::cout << b.toString() << " < " << a.toString() << " -> " << (b < a) << "\n";
        std::cout << b.toString() << " > " << a.toString() << " -> " << (b > a) << "\n";
    }

    {
        thoth::Integer a("2");
        thoth::Integer b("-1");

        std::cout << a.toString() << " < " << b.toString() << " -> " << (a < b) << "\n";
        std::cout << a.toString() << " > " << b.toString() << " -> " << (a > b) << "\n";
        std::cout << b.toString() << " < " << a.toString() << " -> " << (b < a) << "\n";
        std::cout << b.toString() << " > " << a.toString() << " -> " << (b > a) << "\n";
    }

    {
        thoth::Integer a("999");
        thoth::Integer b("998");

        std::cout << a.toString() << " < " << b.toString() << " -> " << (a < b) << "\n";
        std::cout << a.toString() << " > " << b.toString() << " -> " << (a > b) << "\n";
        std::cout << b.toString() << " < " << a.toString() << " -> " << (b < a) << "\n";
        std::cout << b.toString() << " > " << a.toString() << " -> " << (b > a) << "\n";
    }

    {
        thoth::Integer a("1");
        thoth::Integer b("1");

        std::cout << a.toString() << " < " << b.toString() << " -> " << (a < b) << "\n";
        std::cout << a.toString() << " > " << b.toString() << " -> " << (a > b) << "\n";
        std::cout << b.toString() << " < " << a.toString() << " -> " << (b < a) << "\n";
        std::cout << b.toString() << " > " << a.toString() << " -> " << (b > a) << "\n";
    }

    {
        thoth::Integer a("1123");
        thoth::Integer b("-97");

        thoth::Integer c1 = a + b;
        //std::cout << a.toString() << " + " << b.toString() << " = "<< c1.toString() << "\n";

        thoth::Integer c2 = b + a;
        //std::cout << b.toString() << " + " << a.toString() << " = "<< c2.toString() << "\n";
    }

    {
        thoth::Integer a("1");
        thoth::Integer b("-1");

        thoth::Integer c1 = a + b;
        //std::cout << a.toString() << " + " << b.toString() << " = "<< c1.toString() << "\n";

        thoth::Integer c2 = b + a;
        //std::cout << b.toString() << " + " << a.toString() << " = "<< c2.toString() << "\n";
    }

    {
        thoth::Integer a("10");
        thoth::Integer b("-1");

        thoth::Integer c1 = a + b;
        //std::cout << a.toString() << " + " << b.toString() << " = "<< c1.toString() << "\n";

        thoth::Integer c2 = b + a;
        //std::cout << b.toString() << " + " << a.toString() << " = "<< c2.toString() << "\n";
    }

    {
        thoth::Integer a("-12");
        thoth::Integer b("3");

        thoth::Integer c1 = a + b;
        //std::cout << a.toString() << " + " << b.toString() << " = "<< c1.toString() << "\n";

        thoth::Integer c2 = b + a;
        //std::cout << b.toString() << " + " << a.toString() << " = "<< c2.toString() << "\n";
    }

    {
        thoth::Integer a("1");
        thoth::Integer b("1");

        thoth::Integer c = a - b;

        std::cout << a.toString() << " - " << b.toString() << " = "<< c.toString() << "\n";
    }

    {
        thoth::Integer a("999");
        thoth::Integer b("999");

        thoth::Integer c = a - b;

        std::cout << a.toString() << " - " << b.toString() << " = "<< c.toString() << "\n";
    }

    {
        thoth::Integer a("5198465126841256841265412");
        thoth::Integer b("4155984512489156294981564895149815664589152489415");

        thoth::Integer c = a - b;

        std::cout << a.toString() << " - " << b.toString() << " = "<< c.toString() << "\n";
    }

    {
        thoth::Integer a("4155984512489156294981564895149815664589152489415");
        thoth::Integer b("5198465126841256841265412");

        thoth::Integer c = a - b;

        std::cout << a.toString() << " - " << b.toString() << " = "<< c.toString() << "\n";
    }

  
    {
        thoth::Integer a("-4155984512489156294981564895149815664589152489415");
        thoth::Integer b("-519846512684125684126541");

        thoth::Integer c = a - b;

        std::cout << a.toString() << " - " << b.toString() << " = "<< c.toString() << "\n";
    }
    */

    return 0;
}
